export class ChannelVM {
    channelId?: number;  
    channelName?: string;  
    isGroup?: boolean;  
    isAdmin?: boolean;  
    lastMessageContent?: string; 
    lastMessageDate?: Date; 
    isActive: boolean=false;  
    isAvailable: boolean=false;  
    
}
